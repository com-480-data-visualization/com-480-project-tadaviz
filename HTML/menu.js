/*let cards = document.getElementsByName('card')
let current_page

cards.forEach((item, i) => {
  item.addEventListener('mouseover',onMouseover)
  item.addEventListener('mouseout',onMouseout)
});

function onMouseover(){

  cards = document.getElementsByName('card')
  cards.forEach((item, i) => {
    if (item.getAttribute('class') == 'fixcard'){
      current_page = item
    }
    item.setAttribute('class','card')
  });
}

function onMouseout(){
  current_page.setAttribute('class','fixcard')
}*/
